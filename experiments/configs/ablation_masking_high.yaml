name: ablation_masking_high
seed: 42
output_dir: outputs

data:
  masking_base_rate: 0.4

model:
  architecture: addition
  d_embed: 64
  d_model: 128

training:
  loss_name: distance_regression
  lr: 1.0e-3
  max_epochs: 100
  warmup_steps: 1000
