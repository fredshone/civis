name: baseline_addition
seed: 42
output_dir: outputs

model:
  architecture: addition
  d_embed: 64
  d_model: 128
  dropout: 0.1

training:
  loss_name: distance_regression
  loss_kwargs:
    use_huber: false
    normalize_emb_dist: true
  lr: 1.0e-3
  max_epochs: 100
  warmup_steps: 1000
